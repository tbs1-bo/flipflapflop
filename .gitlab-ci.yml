image: alpine

pages:
  script:
  - apk --no-cache add py2-pip python-dev
  - cd docgen && ls && pip install -r requirements.txt
  - apk --no-cache add make
  - cd docgen && make html
  - mv docgen/_build/html/ public/
  artifacts:
    paths:
    - public
  only:
  - master
